import 'package:flutter/material.dart';

// the background color of the app
const Color applicationBackgroundColor = Colors.black;

// the app bar color
const Color appBarColor = Colors.black;

// the color of text in the app
const Color textColor = Colors.white70;

// the color of the buttons in the application
const Color buttonColor = Colors.blueGrey;

//color of the cards or list tiles
const Color cardColor = Colors.redAccent;